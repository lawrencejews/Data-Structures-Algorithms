// Class representing Array

class ArrayStruct {
    constructor() {
        this._storage = '';
    }

    //Pushes a value at the end of the array
    push() {
        
    }

    //Pops off the last value of the array
    pop() {
        
    }

    //Returns the length of the array
    length() {
        
    }
}

const myArray = new ArrayStruct();
console.log(myArray);